options:
    hostname: localhost
    keepalive: false
    open: true
    base:
        - build/public

dev:
    options:
        port: 9000

run:
    options:
        port: 8080
        keepalive: true

test:
    options:
        port: 9090
        open: false

